void setup() {
   background(0);
  size(700, 700);
}

float r = random(50, 100);

void draw() {
 
  //background(0);
  fill(random(0, 255), random(0, 255), random(0, 255));
  triangle(mouseX , mouseY -100, mouseX +100, mouseY +100, mouseX -100, mouseY +100);
  noStroke(); 
}
